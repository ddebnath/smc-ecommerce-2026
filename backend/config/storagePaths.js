import path from "path";

// Base upload directories
export const TEMP_CHUNKS_DIR = path.join(process.cwd(), "temp_chunks");

export const VIDEO_UPLOAD_DIR = path.join(process.cwd(), "uploads/videos");
